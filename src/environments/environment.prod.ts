export const environment = {
  production: true,
  // Usamos una URL relativa al host actual para que funcione en cualquier entorno
  apiUrl: './api',
  vapidPublicKey: '' // Esta clave deberá ser proporcionada por el backend cuando implemente las notificaciones push
};
